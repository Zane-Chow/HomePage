# Deployment target: "com" or "cn". Builds default to "com" when omitted.
SITE_VARIANT=com

# Build-only secret used to resolve node IPs into public coordinates.
# Never prefix this variable with VITE_, or it may be exposed to the browser.
KOMARI_API_KEY=
