# Deployment target: "com" or "cn". Builds default to "com" when omitted.
SITE_VARIANT=com

# Build-only configuration used to resolve node IPs into public coordinates.
# Never prefix these variables with VITE_, or they may be exposed to the browser.
KOMARI_BASE_URL=https://ops.zhouhaoze.top
KOMARI_API_KEY=
